# Инструкция по запуску Admin Panel

## 1. Установка зависимостей

```bash
cd "front admin"
npm install
```

## 2. Настройка переменных окружения

Файл `.env.local` уже создан с настройками:

```env
NEXT_PUBLIC_API_URL=https://api.test-shem.ru/api/v1
NEXT_PUBLIC_MOCK_MODE=true  # Включен мок-режим для тестирования визуала
NEXT_PUBLIC_S3_BUCKET_URL=https://s3.twcstorage.ru/f7eead03-crmfiles
```

## 3. Запуск в режиме разработки

```bash
npm run dev
```

Админка будет доступна по адресу: http://localhost:3004

## 4. Сборка для production

```bash
npm run build
npm start
```

## 5. Запуск через Docker

```bash
docker build -t admin-frontend .
docker run -p 3004:3004 admin-frontend
```

## Структура проекта

```
front admin/
├── src/
│   ├── app/                  # Next.js App Router страницы
│   │   ├── employees/       # Управление сотрудниками
│   │   ├── regions/         # Управление регионами
│   │   ├── reports/         # Отчеты
│   │   ├── profile/         # Профиль
│   │   ├── login/           # Логин
│   │   └── page.tsx         # Главная страница
│   ├── components/          # React компоненты
│   │   ├── navigation.tsx   # Навигация
│   │   └── ui/              # UI компоненты
│   ├── lib/                 # Библиотеки
│   │   ├── api.ts          # API клиент
│   │   └── utils.ts        # Утилиты
│   └── store/              # Zustand stores
│       └── auth.store.ts   # Авторизация
├── public/                 # Статические файлы
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.js
```

## Основные функции

### ✅ Реализовано:
- 🔐 Авторизация (роль: admin)
- 🎨 Дизайн в стиле директора (темная тема с teal акцентами)
- 📊 Главная страница с статистикой
- 📱 Адаптивная навигация
- 🔄 State management (Zustand)
- 🎯 Защита роутов
- 🚀 Готовая структура для расширения

### 🔨 В разработке (заглушки):
- Управление сотрудниками
- Управление регионами
- Отчеты и аналитика
- Финансовые отчеты

## API Endpoints

Админка использует те же API endpoints что и директор:

- `POST /auth/login` - Логин (role: 'admin')
- `POST /auth/logout` - Выход
- `GET /users` - Список сотрудников
- `POST /users` - Создание сотрудника
- `PUT /users/:id` - Редактирование сотрудника
- `DELETE /users/:id` - Удаление сотрудника
- `GET /reports/global` - Глобальная статистика
- `GET /reports/regions` - Статистика по регионам
- `GET /reports/cities` - Статистика по городам

## Доступ (МОК-РЕЖИМ для тестирования визуала)

**Для входа используйте:**
- Логин: `admin`
- Пароль: `admin`

URL: http://localhost:3004 (dev) / https://admin.test-shem.ru (production)
Порт: 3004
Роль: admin (учредитель)

**Отключение мок-режима:**
Для подключения к реальному API измените в `.env.local`:
```env
NEXT_PUBLIC_MOCK_MODE=false
```

## Следующие шаги

1. Добавить реальные данные в статистику на главной странице
2. Реализовать CRUD для сотрудников
3. Добавить управление регионами
4. Реализовать детальные отчеты с графиками
5. Добавить настройки в профиле

